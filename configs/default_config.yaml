model:
  num_frames: 32
  feature_dim: 2048
  flow_dim: 2
  fused_dim: 1024
  transformer_heads: 8
  transformer_layers: 4
  dropout: 0.1

training:
  batch_size: 4
  epochs: 5
  learning_rate: 0.0001
  weight_decay: 0.00001
  triplet_margin: 1.0
  consistency_weight: 0.5
  device: "cuda"

data_dir: "C:\\Users\\adity\\Downloads\\ml-ware-26-sherlock-files"
output_dir: "checkpoints"
use_wandb: true
wandb_project: "video-chronological-sorting"
